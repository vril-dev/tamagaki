config BR2_PACKAGE_TUKUYOMI_EDGE
    bool "tukuyomi-edge"
    help
      External Go binary used for the HTTP reverse proxy / Coraza path.

      Leave this disabled until the matching GitHub Release artifact exists.
